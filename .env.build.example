# =============================================================================
# Docker Build Environment Variables
# =============================================================================
# Este arquivo contém APENAS variáveis NEXT_PUBLIC_* para o build do Docker
# 
# ⚠️ NÃO commite este arquivo no Git! (já está no .gitignore)
# ⚠️ Use apenas valores PÚBLICOS aqui (nunca secrets/API keys)
#
# Copie os valores do seu .env.local e execute:
# ./scripts/docker-build-with-env.sh
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE (OBRIGATÓRIO)
# -----------------------------------------------------------------------------
# Pegue do painel do Supabase: Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=

# -----------------------------------------------------------------------------
# URLs DA APLICAÇÃO (RECOMENDADO)
# -----------------------------------------------------------------------------
# Use os domínios de produção
NEXT_PUBLIC_DASHBOARD_URL=
NEXT_PUBLIC_MEU_PROCESSO_URL=
NEXT_PUBLIC_WEBSITE_URL=
NEXT_PUBLIC_APP_URL=

# -----------------------------------------------------------------------------
# DYTE - VÍDEO/ÁUDIO (OPCIONAL)
# -----------------------------------------------------------------------------
# Necessário apenas se usar chamadas de vídeo/áudio
NEXT_PUBLIC_DYTE_ORG_ID=

# -----------------------------------------------------------------------------
# OUTRAS (OPCIONAL)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_AI_FAKE_STREAMING=false
# NEXT_PUBLIC_FORMSIGN_SUBMIT_ENABLED=true
