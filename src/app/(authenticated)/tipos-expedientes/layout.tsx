import { PageShell } from '@/components/shared/page-shell';

export default function TiposExpedientesModuleLayout({ children }: { children: React.ReactNode }) {
    return <PageShell>{children}</PageShell>;
}
