import { PageShell } from '@/components/shared/page-shell';

export default function NotificacoesModuleLayout({ children }: { children: React.ReactNode }) {
    return <PageShell>{children}</PageShell>;
}
