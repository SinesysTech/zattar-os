/**
 * MAIL MODULE — Service
 *
 * Casos de uso e orquestração do módulo de e-mail.
 * Atualmente a lógica de serviço é feita via API routes (/api/mail/*).
 * Este arquivo serve como ponto de extensão para lógica server-side futura.
 */
