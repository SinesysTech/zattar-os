/**
 * MAIL MODULE — Server Actions
 *
 * Barrel export para server actions do módulo de e-mail.
 * Atualmente as operações são feitas via API routes (/api/mail/*).
 * Este arquivo serve como ponto de extensão para server actions futuras.
 */
