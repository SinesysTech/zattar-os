/**
 * MAIL MODULE — Repository
 *
 * Queries isoladas do módulo de e-mail.
 * Atualmente as queries são feitas via API routes (/api/mail/*).
 * Este arquivo serve como ponto de extensão para queries Supabase futuras.
 */
