'use client';
export function UploadDocumentoDialog({ open: _open, onOpenChange: _onOpenChange, baseSlug: _baseSlug }: { open: boolean; onOpenChange: (o: boolean) => void; baseSlug: string }) {
  return null;
}
