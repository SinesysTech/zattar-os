'use client';
import type { KnowledgeBase } from '../../domain';
export function ConfiguracoesTab({ base: _base, isSuperAdmin: _isSuperAdmin }: { base: KnowledgeBase; isSuperAdmin: boolean }) {
  return <p className="text-muted-foreground">Em construção</p>;
}
