/**
 * Re-export do shared. Os tipos do módulo partes vivem em
 * `@/shared/partes/types` para permitir consumo por rotas públicas
 * (assinatura digital, portal, API) sem cross-group imports.
 */
export * from '@/shared/partes/types'
