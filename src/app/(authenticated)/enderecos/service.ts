/**
 * Re-export do shared. Fonte primária do service de endereços vive em
 * `@/shared/enderecos/service` para permitir consumo por rotas públicas
 * sem cross-group imports.
 */
export * from '@/shared/enderecos/service'
