'use client';

import * as React from 'react';

/**
 * Placeholder de diálogo de preview de mídia (Plate).
 * Alguns kits referenciam este componente como `afterEditable`.
 *
 * Se você quiser a UI completa (preview modal, zoom etc.), podemos evoluir
 * este componente depois; por enquanto ele é no-op para não quebrar o build.
 */
export function MediaPreviewDialog() {
  return null;
}


