/**
 * Calendar Components
 *
 * Os componentes de UI do calendário estão em src/app/app/calendar/components/
 * pois são fortemente acoplados ao App Router (Client Components, DnD, etc.).
 *
 * Este barrel existe para compatibilidade com a estrutura FSD e pode receber
 * componentes reutilizáveis conforme o módulo evoluir.
 */

// Nenhum componente extraível no momento.
// Componentes de UI: src/app/app/calendar/components/
