/**
 * CAPTURA SERVICE PUBLIC API
 *
 * Exporta os serviços disponíveis para o módulo de Captura.
 * A implementação real da orquestração reside em `services/capture-orchestrator.ts`.
 */

export * from './services/capture-orchestrator';
