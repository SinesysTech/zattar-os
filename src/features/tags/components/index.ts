/**
 * Tags Components - Barrel Export
 *
 * Componentes de UI do módulo de tags.
 * Componentes relacionados a tags em outros módulos:
 * - ProcessoTagsDialog → src/features/processos/components/processo-tags-dialog.tsx
 * - TagBadge (primitivo) → src/components/ui/tag-badge.tsx
 */

// Sem componentes específicos neste módulo por enquanto.
// Componentes que consomem o módulo de tags vivem nos módulos que os utilizam
// (ex: processos, contratos) por serem componentes compostos com lógica de domínio desses módulos.
export {};
