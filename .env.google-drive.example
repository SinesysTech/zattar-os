# Configuração de Storage - Google Drive via n8n Webhook
# Copie este arquivo para .env e preencha com suas configurações

# Provider de storage (minio | s3 | aws | google-drive)
STORAGE_PROVIDER=google-drive

# Google Drive via n8n Webhook
# URL do webhook do n8n que processa os arquivos e envia para o Google Drive
GOOGLE_DRIVE_WEBHOOK_URL=https://seu-n8n.com/webhook/google-drive

# Token de autenticação para o webhook (deixe em branco se não usar autenticação)
# Este token será enviado no header Authorization: Bearer {token}
GOOGLE_DRIVE_WEBHOOK_TOKEN=

# Endpoints esperados no webhook do n8n:
# - POST /upload - Recebe o arquivo e faz upload no Google Drive
#   Body: FormData com 'file', 'path', 'contentType'
#   Retorna: { path, url, fileId (ou id) }
#
# - POST /delete - Deleta arquivo do Google Drive
#   Body: { path }
#   Retorna: { success: true }
#
# - GET /get-url?path={path}&expiresIn={seconds} - Obtém URL do arquivo
#   Retorna: { url, expiresAt }
#
# - GET /exists?path={path} - Verifica se arquivo existe
#   Retorna: { exists: true/false }
