---
phase: 05-indicadores-detail-dialog
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx
autonomous: true
requirements:
  - DIALOG-01
  - DIALOG-02
  - DIALOG-03
  - DIALOG-04
  - DIALOG-05
  - DIALOG-06
  - DIALOG-07
  - DIALOG-08
  - DIALOG-09
  - DIALOG-10

must_haves:
  truths:
    - "Dialog abre centrado (max-w-3xl) com max-height 92vh e body scrollavel"
    - "Header fixo exibe: titulo, badge de status, data por extenso e 3 botoes de acao (Sala Virtual primario, Visualizar Ata outline, Abrir PJe outline)"
    - "Botao Visualizar Ata so aparece quando audiencia.urlAtaAudiencia nao for null"
    - "Meta strip em GlassPanel depth-1 exibe Horario, Modalidade, TRT/Grau e Responsavel com avatar"
    - "Secao Processo exibe numero mono com link, tribunal, grau e polos com indicador 'e outros' quando polo*RepresentaVarios=true"
    - "Secao Local/Acesso exibe URL virtual copiavel e/ou endereco presencial com badge de presenca hibrida com texto explicito"
    - "Secao Indicadores exibe AudienciaIndicadorBadges com show para segredo/juizo/designada/documento_ativo"
    - "Secao Preparo exibe PrepScore com showBreakdown=true e size='lg'"
    - "Secao Observacoes exibe texto com whitespace-pre-wrap"
    - "Secao Historico exibe AudienciaTimeline"
    - "Footer fixo tem botao Fechar e Editar Audiencia (desabilitado para capturadas com campos nao permitidos)"
    - "EditarAudienciaDialog continua sendo aberto pelo botao Editar"
  artifacts:
    - path: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      provides: "AudienciaDetailDialog component e AudienciaDetailDialogProps interface"
      exports: ["AudienciaDetailDialog", "AudienciaDetailDialogProps"]
  key_links:
    - from: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      to: "src/app/(authenticated)/audiencias/components/audiencia-indicador-badges.tsx"
      via: "AudienciaIndicadorBadges com show e showPresencaDetail"
      pattern: "AudienciaIndicadorBadges"
    - from: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      to: "src/app/(authenticated)/audiencias/components/audiencia-timeline.tsx"
      via: "AudienciaTimeline com audienciaId e audiencia"
      pattern: "AudienciaTimeline"
    - from: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      to: "src/app/(authenticated)/audiencias/domain.ts"
      via: "buildPjeUrl, isAudienciaCapturada"
      pattern: "buildPjeUrl"
---

<objective>
Criar o componente `AudienciaDetailDialog` — substituto do `AudienciaDetailSheet` — com todas as secoes do mock aprovado e integrando os componentes da plan 02.

Purpose: Este e o componente central da phase 5. Reune todos os dados de uma audiencia em um dialog centrado (D-01, D-04) com header fixo, body scrollavel com secoes GlassPanel (D-02) e footer fixo com acoes. Reutiliza PrepScore existente, os novos AudienciaIndicadorBadges e AudienciaTimeline.

Output: `audiencia-detail-dialog.tsx` criado com interface identica a do sheet (`audienciaId?, audiencia?, open, onOpenChange`) para substituicao drop-in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-indicadores-detail-dialog/05-CONTEXT.md
@.planning/phases/05-indicadores-detail-dialog/05-RESEARCH.md
@.planning/phases/05-indicadores-detail-dialog/05-01-SUMMARY.md
@.planning/phases/05-indicadores-detail-dialog/05-02-SUMMARY.md

<interfaces>
<!-- Componentes disponibilizados pelos planos anteriores -->

From plan 01 — domain.ts:
```typescript
export function buildPjeUrl(trt: string, numeroProcesso: string): string;
// returns `https://pje.${trt.toLowerCase()}.jus.br/consultaprocessual/detalhe-processo/${numeroProcesso}`
export function isAudienciaCapturada(audiencia: Pick<Audiencia, 'idPje'>): boolean;
// returns idPje > 0
```

From plan 02 — audiencia-indicador-badges.tsx:
```typescript
export function AudienciaIndicadorBadges(props: {
  audiencia: Audiencia;
  show?: IndicadorKey[];
  showPresencaDetail?: boolean;
  className?: string;
}): JSX.Element | null;

export const AUDIENCIA_INDICADOR_SHOW_CONFIGS: {
  dialog: undefined;
  card: IndicadorKey[];
  row: IndicadorKey[];
};
```

From plan 02 — audiencia-timeline.tsx:
```typescript
export function AudienciaTimeline(props: {
  audienciaId: number;
  audiencia: Audiencia;
  className?: string;
}): JSX.Element;
```

From existing prep-score.tsx:
```typescript
export function PrepScore(props: {
  audiencia: Audiencia;
  className?: string;
  showBreakdown?: boolean;
  size?: 'sm' | 'md' | 'lg';
}): JSX.Element;
// SIZE_CONFIG.lg = { ring: 64, stroke: 6, text: 'text-sm' }
// NOTE: Mock mostra 84px — se necessario adicionar size='xl' ao PrepScore antes de usar
```

From existing audiencia-detail-sheet.tsx (interface a manter — D-13):
```typescript
export interface AudienciaDetailSheetProps {
  audienciaId?: number;
  audiencia?: Audiencia;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
// Pattern de fetch: shouldFetch = !!audienciaId && !audienciaProp
// Pattern de cancelled flag em useEffect
// useUsuarios() para resolver nomes de responsaveis
```

From existing Dialog (shadcn/ui):
```typescript
import { Dialog, DialogContent } from '@/components/ui/dialog';
// DialogContent aceita className para override de max-w
// Para 3-zone layout: className="sm:max-w-3xl max-h-[92vh] flex flex-col p-0 gap-0"
```

From existing GlassPanel:
```typescript
import { GlassPanel } from '@/components/shared/glass-panel';
// <GlassPanel depth={1} className="p-4"> para secoes do dialog (D-02)
```

From existing editar-audiencia-dialog.tsx:
```typescript
export function EditarAudienciaDialog(props: { audiencia: Audiencia; open: boolean; onOpenChange: (open: boolean) => void; }): JSX.Element;
// Integracao com isAudienciaCapturada para controle de campos editaveis
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AudienciaDetailDialog — structure and header sections</name>
  <files>src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx</files>
  <read_first>
    - src/app/(authenticated)/audiencias/components/audiencia-detail-sheet.tsx (ler completo — base para fetch pattern, dados derivados, estados de loading/error/empty)
    - src/components/ui/dialog.tsx (ver props de DialogContent, como customizar className)
    - src/components/shared/glass-panel.tsx (ver props: depth, className)
    - .mocks/audiencias-detail-dialog.html (referencia visual definitiva — ler e mapear todas as secoes e seus conteudos)
    - src/app/(authenticated)/audiencias/components/prep-score.tsx (verificar se SIZE_CONFIG tem 'xl' ou se precisamos adiciona-lo)
  </read_first>
  <action>
Criar `src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx`.

**Estrutura de alto nivel** (per D-01 e D-04):
```
'use client';
// imports...

export interface AudienciaDetailDialogProps {
  audienciaId?: number;
  audiencia?: Audiencia;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function AudienciaDetailDialog({ audienciaId, audiencia: audienciaProp, open, onOpenChange }) {
  // State idêntico ao sheet: fetchedAudiencia, isLoading, error, isEditDialogOpen
  // useUsuarios() para nomes de responsaveis
  // useEffect com cancelled flag (mesmo pattern do sheet)
  
  const audiencia = audienciaProp || fetchedAudiencia;
  const isPje = audiencia ? isAudienciaCapturada(audiencia) : false;
  const pjeUrl = audiencia ? buildPjeUrl(audiencia.trt, audiencia.numeroProcesso) : '';
  const hasAta = !!(audiencia?.urlAtaAudiencia);

  return (
    <>
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-3xl max-h-[92vh] flex flex-col p-0 gap-0 overflow-hidden [scrollbar-width:thin]">
        {/* ── HEADER (fixo) ── */}
        <div className="flex-shrink-0 px-7 pt-6 pb-4 border-b border-border/20">
          {/* Linha 1: icone + titulo + status badge + X button */}
          {/* Linha 2: data por extenso (format ptBR EEEE dd MMM yyyy) */}
          {/* Linha 3: 3 botoes de acao (D-03):
              - "Entrar na Sala Virtual" variant="default" — link para urlAudienciaVirtual, disabled se null
              - "Visualizar Ata" variant="outline" — link para urlAtaAudiencia, renderizar APENAS se hasAta (D-09)
              - "Abrir PJe" variant="outline" — link para pjeUrl, disabled se !isPje (sem idPje) */}
        </div>

        {/* ── BODY (scrollavel) ── */}
        <div className="flex-1 overflow-y-auto px-7 py-5 space-y-3.5">
          {/* Loading state: spinner ou skeleton */}
          {/* Error state: mensagem de erro */}
          {/* Conteudo: secoes */}
        </div>

        {/* ── FOOTER (fixo) ── */}
        <div className="flex-shrink-0 px-7 py-4 border-t border-border/20 flex items-center justify-between bg-muted/30">
          <Button variant="outline" onClick={() => onOpenChange(false)}>Fechar</Button>
          <Button onClick={() => setIsEditDialogOpen(true)}>
            <Pencil className="size-4" />
            Editar Audiência
          </Button>
        </div>
      </DialogContent>
    </Dialog>
    <EditarAudienciaDialog audiencia={audiencia!} open={isEditDialogOpen} onOpenChange={setIsEditDialogOpen} />
    </>
  );
}
```

**BODY — Secoes** (implementar com GlassPanel depth={1} per D-02):

**1. Meta strip** (DIALOG-02): GlassPanel com grid 4 colunas: Horário (Clock icon + HH:mm – HH:mm), Modalidade (AudienciaModalidadeBadge), TRT/Grau (texto com TRT_NOMES[trt] + GRAU_TRIBUNAL_LABELS[grau]), Responsável (Avatar xs + nome).

**2. Processo** (DIALOG-03): GlassPanel com:
- Numero do processo em `font-mono text-sm` + link externo para processo cockpit se disponivel
- TRT e grau em linha de meta
- Polo Ativo: ParteBadge + nome + se `poloAtivoRepresentaVarios === true`, adicionar `<span className="text-muted-foreground text-xs"> e outros</span>` inline
- Polo Passivo: idem com `poloPassivoRepresentaVarios`

**3. Local/Acesso** (DIALOG-04): GlassPanel com:
- Se `modalidade === 'virtual' || 'hibrida'`: mostrar URL virtual com botao de copiar (usando navigator.clipboard.writeText)
- Se `modalidade === 'presencial' || 'hibrida'`: mostrar endereco formatado (logradouro, numero, complemento, bairro, cidade – UF)
- Se `presencaHibrida !== null`: AudienciaIndicadorBadges com show=['presenca_hibrida'] e showPresencaDetail={true}

**4. Indicadores** (DIALOG-05): GlassPanel com titulo "Indicadores" + AudienciaIndicadorBadges com show={['segredo_justica', 'juizo_digital', 'designada', 'documento_ativo']} sem presenca_hibrida (ja no Local/Acesso). Se nenhum indicador ativo, exibir texto "Nenhum indicador especial".

**5. Preparo** (DIALOG-06): GlassPanel com PrepScore audiencia={audiencia} showBreakdown={true} size="lg". Se PrepScore nao tiver size='xl', usar size='lg' e adicionar className para aumentar o ring visualmente: `[&_svg]:w-20 [&_svg]:h-20` se necessario para se aproximar do mock de 84px. Verificar SIZE_CONFIG antes.

**6. Observacoes** (DIALOG-07): GlassPanel condicional (renderizar apenas se `audiencia.observacoes`). Titulo "Observações" + `<p className="text-sm whitespace-pre-wrap text-muted-foreground">{audiencia.observacoes}</p>`.

**7. Historico de Alteracoes** (DIALOG-08): GlassPanel com titulo "Histórico de Alterações" + AudienciaTimeline audienciaId={audiencia.id} audiencia={audiencia}.

**Botao Editar** no footer: Per D-10, sempre renderizar botao "Editar Audiência", mas se `isPje === true`, o `EditarAudienciaDialog` interno ja cuida de restringir campos editaveis via `isAudienciaCapturada()`. Nao precisamos desabilitar o botao — a dialog de edicao ja sabe o que pode editar.

**Icones a importar**: `Gavel`, `Clock`, `ExternalLink`, `Copy`, `Pencil`, `X`, `MapPin` de lucide-react.
**Componentes a importar**: `Dialog`, `DialogContent` de `@/components/ui/dialog`, `Button` de `@/components/ui/button`, `GlassPanel` de `@/components/shared/glass-panel`, `Avatar`, `AvatarImage`, `AvatarFallback` de `@/components/ui/avatar`, `ParteBadge` de `@/components/ui/parte-badge`, `AudienciaStatusBadge` e `AudienciaModalidadeBadge` de arquivos locais, `PrepScore` de `./prep-score`, `AudienciaIndicadorBadges` de `./audiencia-indicador-badges`, `AudienciaTimeline` de `./audiencia-timeline`, `EditarAudienciaDialog` de `./editar-audiencia-dialog`.
**Do domain**: `Audiencia`, `GRAU_TRIBUNAL_LABELS`, `TRT_NOMES`, `isAudienciaCapturada`, `buildPjeUrl`, `StatusAudiencia`, `ModalidadeAudiencia`.
**Hooks**: `useUsuarios` de `@/app/(authenticated)/usuarios`, `actionBuscarAudienciaPorId` de `../actions`.
**Date**: `format`, `parseISO` de `date-fns`, `ptBR` de `date-fns/locale`.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && grep -c "AudienciaTimeline\|AudienciaIndicadorBadges\|PrepScore\|GlassPanel\|buildPjeUrl\|whitespace-pre-wrap" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export.*AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 2 linhas (interface + function)
    - `grep "sm:max-w-3xl" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 1 linha
    - `grep "max-h-\[92vh\]" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 1 linha
    - `grep "flex-1 overflow-y-auto" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 1 linha (body scrollavel)
    - `grep "AudienciaTimeline" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 2 linhas (import + uso)
    - `grep "AudienciaIndicadorBadges" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 2 linhas
    - `grep "buildPjeUrl" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 2 linhas
    - `grep "urlAtaAudiencia" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 2 linhas (check condicional + uso)
    - `grep "whitespace-pre-wrap" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 1 linha (secao observacoes)
    - `grep "EditarAudienciaDialog" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna ao menos 2 linhas
    - Arquivo tem mais de 150 linhas
  </acceptance_criteria>
  <done>AudienciaDetailDialog criado com todas as 7 secoes, header com 3 botoes de acao, body scrollavel e footer com edicao</done>
</task>

<task type="auto">
  <name>Task 2: Type check and architecture validation</name>
  <files></files>
  <read_first>
    - (nenhum arquivo adicional — validacao de compilacao e arquitetura)
  </read_first>
  <action>
Executar verificacoes de qualidade no novo componente:

```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | grep "audiencia-detail-dialog" | head -20
```

```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run check:architecture 2>&1 | tail -10
```

Se houver erros de tipo em `audiencia-detail-dialog.tsx`, corrigir antes de prosseguir. Erros pre-existentes em outros modulos podem ser ignorados.

Verificar que nao ha imports cross-deep de outros modulos:
```bash
grep "from '@/app/(authenticated)/[a-z]*/" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx | grep -v "audiencias\|usuarios"
```

Qualquer import de outro modulo que nao seja barrel deve ser corrigido para usar o barrel export.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | grep "audiencia-detail-dialog" | grep -i "error" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` retorna 0 erros com "audiencia-detail-dialog" no path
    - `npm run check:architecture` nao exibe violacoes relacionadas ao novo arquivo
    - Nenhum cross-deep import de outros modulos (exceto `@/app/(authenticated)/usuarios` que e o barrel correto)
  </acceptance_criteria>
  <done>audiencia-detail-dialog.tsx passa em type check e validacao de arquitetura FSD</done>
</task>

</tasks>

<verification>
1. `grep "sm:max-w-3xl max-h-\[92vh\]" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 1 resultado
2. `grep "flex-shrink-0" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 2 resultados (header + footer)
3. `grep "flex-1 overflow-y-auto" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` retorna 1 resultado (body)
4. `grep "AudienciaTimeline" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` mostra import e uso
5. `grep "Visualizar Ata\|urlAtaAudiencia" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` mostra botao condicional
6. `grep "Abrir PJe\|buildPjeUrl" src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx` mostra botao e construcao de URL
7. `npm run type-check` limpo no arquivo
</verification>

<success_criteria>
- Dialog abre centrado com max-w-3xl e max-h-[92vh]
- Header: titulo + status badge + 3 botoes (sala, ata condicional, pje)
- Meta strip em GlassPanel: horario, modalidade, trt/grau, responsavel com avatar
- Secao Processo: numero mono, tribunal, grau, polos com "e outros" quando aplicavel
- Secao Local/Acesso: URL copiavel e/ou endereco, badge presenca hibrida com texto explicito
- Secao Indicadores: 4 badges via AudienciaIndicadorBadges
- Secao Preparo: PrepScore showBreakdown=true size='lg'
- Secao Observacoes: whitespace-pre-wrap, renderiza apenas se tem observacoes
- Secao Historico: AudienciaTimeline integrado
- Footer: Fechar + Editar (abre EditarAudienciaDialog)
- Type check e arquitetura FSD ok
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-indicadores-detail-dialog/05-03-SUMMARY.md`
</output>
