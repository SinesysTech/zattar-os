---
phase: 05-indicadores-detail-dialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/design-system/variants.ts
  - src/app/(authenticated)/audiencias/domain.ts
autonomous: true
requirements:
  - INDIC-01
  - INDIC-02
  - INDIC-03
  - INDIC-04
  - INDIC-05
  - INDIC-06
  - DIALOG-02
  - DIALOG-03
  - DIALOG-09

must_haves:
  truths:
    - "SemanticBadge aceita category='audiencia_indicador' e renderiza variantes soft corretas"
    - "domain.ts exporta AUDIENCIA_FIELD_LABELS com labels legiveis para campos de audiencia"
    - "domain.ts exporta buildPjeUrl(trt, numeroProcesso) retornando URL PJe valida"
    - "npm run type-check passa sem erros apos mudancas"
  artifacts:
    - path: "src/lib/design-system/variants.ts"
      provides: "BadgeCategory com audiencia_indicador, AUDIENCIA_INDICADOR_VARIANTS, case no switch, soft tone"
      contains: "audiencia_indicador"
    - path: "src/app/(authenticated)/audiencias/domain.ts"
      provides: "AUDIENCIA_FIELD_LABELS, buildPjeUrl"
      exports:
        - "AUDIENCIA_FIELD_LABELS"
        - "buildPjeUrl"
  key_links:
    - from: "src/lib/design-system/variants.ts"
      to: "src/components/ui/semantic-badge.tsx"
      via: "BadgeCategory type consumed by SemanticBadge"
      pattern: "audiencia_indicador"
---

<objective>
Extender o design system com a categoria de badge `audiencia_indicador` e adicionar helpers de dominio necessarios para as plans subsequentes.

Purpose: Estabelecer os contratos de dados e visuais que todos os planos seguintes consomem. Badges de indicadores de audiencia precisam da categoria no design system antes de qualquer componente poder usa-los. Labels e buildPjeUrl precisam existir no domain antes da dialog poder renderizar timeline e abrir PJe.

Output: `variants.ts` atualizado com nova categoria e mapeamentos; `domain.ts` atualizado com AUDIENCIA_FIELD_LABELS e buildPjeUrl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-indicadores-detail-dialog/05-CONTEXT.md
@.planning/phases/05-indicadores-detail-dialog/05-RESEARCH.md

<interfaces>
<!-- Contrato atual de variants.ts (relevante para extensao) -->

From src/lib/design-system/variants.ts:
```typescript
export type BadgeCategory =
  | 'tribunal'
  | 'status'
  | 'grau'
  | 'parte'
  | 'audiencia_status'
  | 'audiencia_modalidade'
  | 'expediente_tipo'
  | 'captura_status'
  | 'polo'
  | 'tipo_contrato'
  | 'tipo_cobranca'
  | 'status_contrato'
  | 'folha_status'
  | 'salario_status'
  | 'call_status'
  | 'network_quality'
  | 'online_status'
  | 'obrigacao_status'
  | 'obrigacao_tipo'
  | 'obrigacao_direcao'
  | 'document_signature_status'
  | 'project_status'
  | 'task_status'
  | 'priority'
  | 'template_status'
  | 'ativo_status'
  | 'expediente_status'
  | 'payment_status'
  | 'financial_alert'
  | 'error_type'
  | 'pericia_situacao'
  | 'parcela_status'
  | 'repasse_status'
  | 'orcamento_status'
  | 'tipo_conta_contabil'
  | 'conciliacao_status'
  | 'orcamento_item_status';

export function getSemanticBadgeTone(category: BadgeCategory, _value?: string | number | null): BadgeTone {
  const softCategories: BadgeCategory[] = [
    'tipo_cobranca', 'tipo_contrato', 'captura_status', 'status_contrato',
    'audiencia_status', 'template_status', 'ativo_status', 'expediente_status',
    'document_signature_status', 'task_status', 'payment_status', 'financial_alert',
    'error_type', 'pericia_situacao', 'parcela_status', /* ...more */
  ];
  if (softCategories.includes(category)) return 'soft';
  return 'solid';
}

export function getSemanticBadgeVariant(
  category: BadgeCategory,
  key: string | number | null | undefined
): BadgeVisualVariant { /* switch(category) { case 'audiencia_status': ... } */ }
```

From src/app/(authenticated)/audiencias/domain.ts:
```typescript
export interface Audiencia {
  id: number; idPje: number; trt: CodigoTribunal; grau: GrauTribunal;
  numeroProcesso: string; dataInicio: string; dataFim: string;
  horaInicio: string | null; horaFim: string | null;
  modalidade: ModalidadeAudiencia | null; presencaHibrida: PresencaHibrida | null;
  status: StatusAudiencia; statusDescricao: string | null;
  designada: boolean; emAndamento: boolean; documentoAtivo: boolean;
  segredoJustica: boolean; juizoDigital: boolean;
  poloAtivoNome: string | null; poloPassivoNome: string | null;
  poloAtivoRepresentaVarios: boolean; poloPassivoRepresentaVarios: boolean;
  urlAudienciaVirtual: string | null; enderecoPresencial: EnderecoPresencial | null;
  responsavelId: number | null; observacoes: string | null;
  dadosAnteriores: Record<string, unknown> | null;
  ataAudienciaId: number | null; urlAtaAudiencia: string | null;
  createdAt: string; updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SemanticBadge design system with audiencia_indicador category</name>
  <files>src/lib/design-system/variants.ts</files>
  <read_first>
    - src/lib/design-system/variants.ts (arquivo completo — ler para entender estrutura do switch e softCategories)
    - src/lib/design-system/tokens.ts (verificar variantas visuais disponiveis: warning, info, success, neutral, accent)
  </read_first>
  <action>
Adicionar suporte a `audiencia_indicador` em TRES lugares do arquivo `variants.ts`:

**1. Adicionar ao union type `BadgeCategory`** (apos `'orcamento_item_status'`):
```typescript
  | 'audiencia_indicador';
```

**2. Criar constante de mapeamento ANTES de `getSemanticBadgeVariant`**:
```typescript
export const AUDIENCIA_INDICADOR_VARIANTS: Record<string, BadgeVisualVariant> = {
  segredo_justica: 'warning',
  juizo_digital: 'info',
  designada: 'success',
  documento_ativo: 'info',
  litisconsorcio: 'neutral',
  presenca_hibrida: 'accent',
};
```

**3. Adicionar case no switch dentro de `getSemanticBadgeVariant`** (apos o ultimo case existente, antes do `default`):
```typescript
case 'audiencia_indicador':
  return AUDIENCIA_INDICADOR_VARIANTS[key as string] ??
    AUDIENCIA_INDICADOR_VARIANTS[normalizedKey] ??
    'neutral';
```

**4. Adicionar `'audiencia_indicador'` ao array `softCategories`** dentro de `getSemanticBadgeTone` (append apos o ultimo item existente).

IMPORTANTE: Ler o arquivo completo antes de editar para nao perder os cases ja existentes no switch. Usar Edit tool para insercoes cirurgicas.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && grep -n "audiencia_indicador" src/lib/design-system/variants.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "audiencia_indicador" src/lib/design-system/variants.ts` retorna exatamente 4 linhas (union type, VARIANTS const, switch case, softCategories)
    - `grep "AUDIENCIA_INDICADOR_VARIANTS" src/lib/design-system/variants.ts` retorna ao menos 8 linhas (definicao + 6 entradas + uso no case)
    - `grep "'warning'" src/lib/design-system/variants.ts` inclui linha com `segredo_justica`
    - `grep "'accent'" src/lib/design-system/variants.ts` inclui linha com `presenca_hibrida`
  </acceptance_criteria>
  <done>variants.ts aceita category='audiencia_indicador' no tipo e mapeamentos corretos para os 6 indicadores</done>
</task>

<task type="auto">
  <name>Task 2: Add AUDIENCIA_FIELD_LABELS and buildPjeUrl to domain.ts</name>
  <files>src/app/(authenticated)/audiencias/domain.ts</files>
  <read_first>
    - src/app/(authenticated)/audiencias/domain.ts (arquivo completo — verificar onde inserir apos as constantes existentes)
  </read_first>
  <action>
Adicionar duas exportacoes no final do arquivo `domain.ts`, apos as constantes existentes (`STATUS_AUDIENCIA_LABELS`, `MODALIDADE_AUDIENCIA_LABELS`, `GRAU_TRIBUNAL_LABELS`):

**1. Adicionar AUDIENCIA_FIELD_LABELS** — Record mapeando nomes de campos snake_case para labels legiveis em PT-BR:
```typescript
/**
 * Labels legíveis para campos de audiência — usados na timeline de alterações.
 * Chaves são snake_case (formato de dados_anteriores e logs_alteracao).
 */
export const AUDIENCIA_FIELD_LABELS: Record<string, string> = {
  data_inicio: 'Data/Horário de Início',
  data_fim: 'Data/Horário de Fim',
  hora_inicio: 'Hora de Início',
  hora_fim: 'Hora de Fim',
  responsavel_id: 'Responsável',
  status: 'Status',
  status_descricao: 'Situação',
  modalidade: 'Modalidade',
  observacoes: 'Observações',
  url_audiencia_virtual: 'URL da Sala Virtual',
  endereco_presencial: 'Endereço Presencial',
  tipo_audiencia_id: 'Tipo de Audiência',
  sala_audiencia_nome: 'Sala',
  designada: 'Designada',
  segredo_justica: 'Segredo de Justiça',
  juizo_digital: 'Juízo Digital',
  documento_ativo: 'Documento Ativo',
  presenca_hibrida: 'Presença Híbrida',
  polo_ativo_nome: 'Polo Ativo',
  polo_passivo_nome: 'Polo Passivo',
  numero_processo: 'Número do Processo',
  url_ata_audiencia: 'URL da Ata',
};
```

**2. Adicionar buildPjeUrl** — helper para construir URL de consulta PJe:
```typescript
/**
 * Constrói a URL de consulta processual no PJe a partir do TRT e número do processo.
 * Formato: https://pje.{trt_lower}.jus.br/consultaprocessual/detalhe-processo/{numeroProcesso}
 *
 * @example buildPjeUrl('TRT1', '0001234-56.2024.5.01.0001')
 * // returns 'https://pje.trt1.jus.br/consultaprocessual/detalhe-processo/0001234-56.2024.5.01.0001'
 */
export function buildPjeUrl(trt: string, numeroProcesso: string): string {
  const trtLower = trt.toLowerCase();
  return `https://pje.${trtLower}.jus.br/consultaprocessual/detalhe-processo/${numeroProcesso}`;
}
```

Inserir ao final do arquivo, apos os blocos de constantes existentes.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && grep -c "AUDIENCIA_FIELD_LABELS\|buildPjeUrl" src/app/(authenticated)/audiencias/domain.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const AUDIENCIA_FIELD_LABELS" src/app/(authenticated)/audiencias/domain.ts` retorna 1 linha
    - `grep "export function buildPjeUrl" src/app/(authenticated)/audiencias/domain.ts` retorna 1 linha
    - `grep "data_inicio" src/app/(authenticated)/audiencias/domain.ts` retorna linha com label 'Data/Horário de Início'
    - `grep "trt.toLowerCase" src/app/(authenticated)/audiencias/domain.ts` retorna 1 linha
  </acceptance_criteria>
  <done>domain.ts exporta AUDIENCIA_FIELD_LABELS (22 campos) e buildPjeUrl(trt, numeroProcesso)</done>
</task>

<task type="auto">
  <name>Task 3: Type check validation</name>
  <files></files>
  <read_first>
    - (nenhum arquivo adicional necessario — validacao de compilacao)
  </read_first>
  <action>
Executar type check para garantir que as extensoes nao quebraram a tipagem:

```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | tail -20
```

Se houver erros relacionados a `audiencia_indicador` ou `AUDIENCIA_FIELD_LABELS`, corrigir antes de prosseguir. Erros em outros arquivos sem relacao com as mudancas desta plan podem ser ignorados (sao pre-existentes).
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | grep -E "variants\.ts|domain\.ts" | grep -i error | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` nao exibe erros em `variants.ts` ou `domain.ts`
    - O comando retorna 0 linhas de error relacionadas aos arquivos modificados nesta plan
  </acceptance_criteria>
  <done>Type check passa sem erros nos arquivos modificados por esta plan</done>
</task>

</tasks>

<verification>
1. `grep -c "audiencia_indicador" src/lib/design-system/variants.ts` retorna 4
2. `grep "AUDIENCIA_INDICADOR_VARIANTS" src/lib/design-system/variants.ts` mostra mapeamento com 6 entradas
3. `grep "export const AUDIENCIA_FIELD_LABELS" src/app/(authenticated)/audiencias/domain.ts` retorna 1 resultado
4. `grep "export function buildPjeUrl" src/app/(authenticated)/audiencias/domain.ts` retorna 1 resultado
5. `npm run type-check` nao emite erros em variants.ts ou domain.ts
</verification>

<success_criteria>
- SemanticBadge com `category="audiencia_indicador"` renderiza com tone `soft` (nao solid)
- Variantes corretas: segredo_justica=warning, juizo_digital=info, designada=success, documento_ativo=info, litisconsorcio=neutral, presenca_hibrida=accent
- AUDIENCIA_FIELD_LABELS exportado com 22+ campos mapeados
- buildPjeUrl('TRT15', '0001234') retorna 'https://pje.trt15.jus.br/consultaprocessual/detalhe-processo/0001234'
- Type check limpo nos dois arquivos modificados
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-indicadores-detail-dialog/05-01-SUMMARY.md`
</output>
