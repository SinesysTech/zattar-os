---
phase: 05-indicadores-detail-dialog
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/app/(authenticated)/audiencias/components/index.ts
  - src/app/(authenticated)/audiencias/index.ts
  - src/app/(authenticated)/audiencias/audiencias-client.tsx
  - src/app/(authenticated)/audiencias/components/audiencias-content.tsx
  - src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx
  - src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx
  - src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx
autonomous: false
requirements:
  - DIALOG-01
  - DIALOG-10

must_haves:
  truths:
    - "Zero referencias a AudienciaDetailSheet ou audiencia-detail-sheet no codigo fonte"
    - "AudienciaDetailDialog exportado pelo barrel de componentes e pelo barrel do modulo"
    - "AudienciaIndicadorBadges exportado pelo barrel de componentes (para uso na Phase 6)"
    - "npm run type-check passa sem erros apos a migracao"
    - "npm run check:architecture nao exibe violacoes"
    - "Teste de audiencias-content continua passando (mock atualizado)"
  artifacts:
    - path: "src/app/(authenticated)/audiencias/components/index.ts"
      provides: "Barrel de componentes atualizado com Dialog no lugar de Sheet"
      contains: "AudienciaDetailDialog"
    - path: "src/app/(authenticated)/audiencias/index.ts"
      provides: "Barrel do modulo atualizado"
      contains: "AudienciaDetailDialog"
  key_links:
    - from: "src/app/(authenticated)/audiencias/audiencias-client.tsx"
      to: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      via: "import via barrel @/app/(authenticated)/audiencias"
      pattern: "AudienciaDetailDialog"
    - from: "src/app/(authenticated)/audiencias/components/audiencias-content.tsx"
      to: "src/app/(authenticated)/audiencias/components/audiencia-detail-dialog.tsx"
      via: "import direto ./audiencia-detail-dialog"
      pattern: "AudienciaDetailDialog"
---

<objective>
Big-bang migration: substituir todas as referencias a `AudienciaDetailSheet` por `AudienciaDetailDialog` nos 5 pontos de uso e nos 2 barrels, e atualizar o mock do teste.

Purpose: O novo dialog so estara funcional quando todos os pontos de uso apontarem para ele. A interface de props e identica (D-13), entao cada ponto de uso e uma troca de nome simples. O arquivo original nao e deletado neste plano (pode ser mantido ou renomeado — a migration big-bang e via update de imports).

Output: Zero referencias a AudienciaDetailSheet em imports e JSX; type check limpo; testes passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-indicadores-detail-dialog/05-CONTEXT.md
@.planning/phases/05-indicadores-detail-dialog/05-03-SUMMARY.md

<interfaces>
<!-- Interface do novo dialog (identica ao sheet — D-13) -->

From plan 03 — audiencia-detail-dialog.tsx:
```typescript
export interface AudienciaDetailDialogProps {
  audienciaId?: number;
  audiencia?: Audiencia;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
export function AudienciaDetailDialog(props: AudienciaDetailDialogProps): JSX.Element;
```

<!-- Pontos de uso a migrar (D-12) -->

1. src/app/(authenticated)/audiencias/audiencias-client.tsx
   - import: `AudienciaDetailSheet` de `@/app/(authenticated)/audiencias`
   - JSX: `<AudienciaDetailSheet open={isDetailOpen} onOpenChange={setIsDetailOpen} audiencia={selectedAudiencia} />`

2. src/app/(authenticated)/audiencias/components/audiencias-content.tsx
   - import: `AudienciaDetailSheet` de `./audiencia-detail-sheet`
   - JSX: `<AudienciaDetailSheet open={isMissionDetailOpen} onOpenChange={setIsMissionDetailOpen} ...>`

3. src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx
   - import: `AudienciaDetailSheet` de `./audiencia-detail-sheet`
   - JSX: `<AudienciaDetailSheet open={detailOpen} onOpenChange={setDetailOpen} ...>`

4. src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx
   - import: `AudienciaDetailSheet` de `./audiencia-detail-sheet`
   - JSX: `<AudienciaDetailSheet open={detailOpen} onOpenChange={setDetailOpen} ...>`

5. src/app/(authenticated)/audiencias/components/index.ts
   - export: `{ AudienciaDetailSheet } from './audiencia-detail-sheet'`

6. src/app/(authenticated)/audiencias/index.ts
   - export: `AudienciaDetailSheet` from components

7. src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx
   - mock: `jest.mock('../audiencia-detail-sheet', () => ({ AudienciaDetailSheet: jest.fn(() => null) }))`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update barrel exports and migrate all 5 usage points</name>
  <files>
    src/app/(authenticated)/audiencias/components/index.ts,
    src/app/(authenticated)/audiencias/index.ts,
    src/app/(authenticated)/audiencias/audiencias-client.tsx,
    src/app/(authenticated)/audiencias/components/audiencias-content.tsx,
    src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx,
    src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx,
    src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx
  </files>
  <read_first>
    - src/app/(authenticated)/audiencias/components/index.ts (ver exports atuais para adicionar Dialog e manter os outros)
    - src/app/(authenticated)/audiencias/index.ts (ver exports atuais para atualizar AudienciaDetailSheet)
    - src/app/(authenticated)/audiencias/audiencias-client.tsx (ver import e JSX de AudienciaDetailSheet)
    - src/app/(authenticated)/audiencias/components/audiencias-content.tsx (ver import e JSX de AudienciaDetailSheet)
    - src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx (ver import e JSX)
    - src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx (ver import e JSX)
    - src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx (ver mock de AudienciaDetailSheet)
  </read_first>
  <action>
Realizar as seguintes trocas cirurgicas usando o Edit tool em cada arquivo:

**1. `src/app/(authenticated)/audiencias/components/index.ts`**:
- Substituir linha: `export { AudienciaDetailSheet } from './audiencia-detail-sheet';`
- Por: `export { AudienciaDetailDialog } from './audiencia-detail-dialog';`
- Adicionar APOS a linha do Dialog: `export { AudienciaIndicadorBadges, AUDIENCIA_INDICADOR_SHOW_CONFIGS } from './audiencia-indicador-badges';`
- Adicionar tambem: `export { AudienciaTimeline } from './audiencia-timeline';`

**2. `src/app/(authenticated)/audiencias/index.ts`**:
- Substituir `AudienciaDetailSheet,` por `AudienciaDetailDialog,` no bloco de exports de componentes
- Adicionar `AudienciaIndicadorBadges,` e `AUDIENCIA_INDICADOR_SHOW_CONFIGS,` ao mesmo bloco
- Atualizar tambem os exports de types: remover `AudienciaDetailSheetProps` se existir, adicionar `AudienciaDetailDialogProps`
- Verificar se `AUDIENCIA_FIELD_LABELS` e `buildPjeUrl` precisam ser adicionados ao barrel de tipos/constantes do modulo

**3. `src/app/(authenticated)/audiencias/audiencias-client.tsx`**:
- Substituir `AudienciaDetailSheet,` por `AudienciaDetailDialog,` no import
- Substituir `<AudienciaDetailSheet` por `<AudienciaDetailDialog` no JSX
- Substituir `</AudienciaDetailSheet>` por `</AudienciaDetailDialog>` se aplicavel (auto-fechamento nao precisa)

**4. `src/app/(authenticated)/audiencias/components/audiencias-content.tsx`**:
- Substituir `import { AudienciaDetailSheet } from './audiencia-detail-sheet';` por `import { AudienciaDetailDialog } from './audiencia-detail-dialog';`
- Substituir `<AudienciaDetailSheet` por `<AudienciaDetailDialog` no JSX

**5. `src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx`**:
- Substituir import e JSX da mesma forma (Sheet -> Dialog, de `./audiencia-detail-sheet` para `./audiencia-detail-dialog`)

**6. `src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx`**:
- Substituir import e JSX da mesma forma

**7. `src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx`**:
- Substituir `jest.mock('../audiencia-detail-sheet', () => ({ AudienciaDetailSheet: jest.fn(() => null) }))` por `jest.mock('../audiencia-detail-dialog', () => ({ AudienciaDetailDialog: jest.fn(() => null) }))`
- Se houver referencia a `AudienciaDetailSheet` no corpo do teste, substituir por `AudienciaDetailDialog`

**VERIFICACAO POS-SUBSTITUICAO**: Executar:
```bash
grep -rn "AudienciaDetailSheet\|audiencia-detail-sheet" src/app/(authenticated)/audiencias/
```
Deve retornar APENAS o arquivo original `audiencia-detail-sheet.tsx` (o arquivo em si). Nenhuma outra referencia deve existir.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && grep -rn "AudienciaDetailSheet\|audiencia-detail-sheet" src/app/(authenticated)/audiencias/ | grep -v "audiencia-detail-sheet.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AudienciaDetailSheet" src/ | grep -v "audiencia-detail-sheet.tsx"` retorna 0 linhas
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/index.ts` retorna 1 linha de export
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/index.ts` retorna ao menos 1 linha de export
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/audiencias-client.tsx` retorna ao menos 1 linha
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/audiencias-content.tsx` retorna ao menos 1 linha
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/audiencias-list-wrapper.tsx` retorna ao menos 1 linha
    - `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/audiencias-table-wrapper.tsx` retorna ao menos 1 linha
    - `grep "audiencia-detail-dialog" src/app/(authenticated)/audiencias/components/__tests__/audiencias-content.test.tsx` retorna ao menos 1 linha (mock atualizado)
    - `grep "AudienciaIndicadorBadges" src/app/(authenticated)/audiencias/components/index.ts` retorna 1 linha de export
  </acceptance_criteria>
  <done>Zero referencias a AudienciaDetailSheet nos 6 arquivos migrados; AudienciaDetailDialog exportado em todos os barrels necessarios</done>
</task>

<task type="auto">
  <name>Task 2: Final validation — type check, architecture and tests</name>
  <files></files>
  <read_first>
    - (nenhum arquivo adicional — validacao final)
  </read_first>
  <action>
Executar validacao completa do modulo pos-migracao:

**1. Type check**:
```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | grep -E "audiencias/" | grep -i error | head -20
```

**2. Architecture check**:
```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run check:architecture 2>&1 | tail -15
```

**3. Unit tests do modulo**:
```bash
cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm test -- --testPathPattern="audiencias" 2>&1 | tail -20
```

Se houver erros:
- Type errors em arquivos migrados: corrigir as substituicoes
- Architecture violations: verificar se algum import nao usa barrel
- Test failures: verificar se mocks estao corretos no arquivo de test

Apos todos os checks passarem, verificar ausencia total de referencias ao sheet:
```bash
grep -rn "AudienciaDetailSheet\|audiencia-detail-sheet" src/ | grep -v "audiencia-detail-sheet.tsx"
```
Resultado deve ser vazio.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os/.claude/worktrees/tender-banzai && npm run type-check 2>&1 | grep -E "audiencias/(audiencias-client|components/(audiencias-content|audiencias-list|audiencias-table))" | grep -i error | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` retorna 0 erros nos arquivos migrados
    - `npm run check:architecture` nao exibe violacoes
    - `npm test -- --testPathPattern="audiencias"` nao exibe FAIL (pode ter warnings)
    - `grep -rn "AudienciaDetailSheet" src/ | grep -v "audiencia-detail-sheet.tsx"` retorna 0 linhas
  </acceptance_criteria>
  <done>Migracao Sheet→Dialog completa: type check limpo, arquitetura valida, testes passando</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — dialog opens correctly in all views</name>
  <action>Human verification step — no automated action. Run `npm run dev` if not already running, then follow the how-to-verify steps below.</action>
  <what-built>
    Phase 5 completa: SemanticBadge com categoria audiencia_indicador, AudienciaIndicadorBadges, AudienciaTimeline, AudienciaDetailDialog com todas as secoes, e migracao de todos os 5 pontos de uso do Sheet para o Dialog.
  </what-built>
  <how-to-verify>
    1. Abrir a aplicacao em http://localhost:3000/audiencias (executar `npm run dev` se necessario)
    2. Clicar em qualquer audiencia na view Quadro/Missao — verificar que abre um DIALOG CENTRADO (nao sheet lateral)
    3. Verificar no dialog:
       - Header: titulo, badge de status, data, 3 botoes (sala virtual, ata se disponivel, PJe)
       - Meta strip: horario, modalidade, tribunal e responsavel
       - Secao Processo: numero mono, TRT/Grau, polo ativo e passivo
       - Secao Indicadores: badges para indicadores ativos (segredo, juizo, designada, documento)
       - Secao Preparo: ring SVG com checklist expandida
       - Secao Historico: timeline de alteracoes (pode estar vazia com apenas "Captura inicial")
    4. Verificar body scrollavel quando o conteudo excede a viewport
    5. Clicar "Fechar" — dialog fecha
    6. Clicar "Editar Audiência" — dialog de edicao abre
    7. Navegar para Lista e Semana view — clicar numa audiencia, verificar que o dialog abre tambem
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o dialog funcionar corretamente, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
1. `grep -rn "AudienciaDetailSheet" src/ | grep -v "audiencia-detail-sheet.tsx"` retorna vazio
2. `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/components/index.ts` retorna 1 export
3. `grep "AudienciaDetailDialog" src/app/(authenticated)/audiencias/index.ts` retorna ao menos 1 export
4. `npm run type-check 2>&1 | grep "audiencias" | grep error` retorna vazio
5. `npm run check:architecture 2>&1` nao exibe violacoes
6. `npm test -- --testPathPattern="audiencias"` passa
</verification>

<success_criteria>
- Zero referencias a AudienciaDetailSheet em qualquer arquivo exceto o arquivo fonte original
- AudienciaDetailDialog acessivel via barrel do modulo para uso externo
- AudienciaIndicadorBadges exportado para consumo pela Phase 6 (views)
- Type check limpo em todos os arquivos modificados
- Testes do modulo passando
- Human verify: dialog abre centrado ao clicar audiencia em qualquer view
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-indicadores-detail-dialog/05-04-SUMMARY.md`
</output>
