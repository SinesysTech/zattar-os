---
phase: 04-detail-panel-preservation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [PRES-01, PRES-02, PRES-03, PRES-04, PRES-05, PRES-06, PRES-07]

must_haves:
  truths:
    - "User can type and send text messages successfully"
    - "User can record and send audio messages"
    - "User can upload and send files"
    - "User can start a video or audio call"
    - "Typing indicator appears when the remote user is typing"
    - "Real-time messages arrive without page refresh"
    - "Date separators appear correctly between message groups"
    - "ChatContextBar appears when conversation has a linked processo"
    - "ChatEmptyState renders when no conversation is selected on desktop"
    - "On mobile: sidebar hides when chat is selected; UserDetailSheet opens as floating sheet"
    - "ChatDetailPanel opens/closes when clicking header avatar area on xl+ screens"
    - "Lazy loading of ChatWindow and Suspense skeleton preserved"
  artifacts:
    - path: "src/app/(authenticated)/chat/components/chat-window.tsx"
      provides: "All existing chat functionality preserved"
      contains: "UserDetailSheet"
    - path: "src/app/(authenticated)/chat/components/chat-detail-panel.tsx"
      provides: "Inline detail panel for xl+ screens"
      exports: ["ChatDetailPanel"]
  key_links:
    - from: "Header avatar click"
      to: "showProfileSheet state"
      via: "toggleProfileSheet in useChatStore"
      pattern: "toggleProfileSheet"
    - from: "ChatWindow"
      to: "UserDetailSheet"
      via: "import and render"
      pattern: "UserDetailSheet"
---

<objective>
Verify zero regression across all existing chat features after phases 01-04 changes. Run the automated test suite, then perform a structured human walkthrough of the critical flows.

Purpose: Confirm that phases 01-03 refactoring and phase 04 detail panel wiring did not break any existing functionality. This is the gate before marking the v1.0 Chat Redesign milestone complete.
Output: All features verified working; phase marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated checks</name>
  <files></files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-window.tsx (confirm UserDetailSheet still imported)
    - src/app/(authenticated)/chat/components/chat-layout.tsx (confirm Suspense boundary and lazy ChatWindow preserved)
  </read_first>
  <action>
Run all automated checks to validate no regressions were introduced:

1. TypeScript type check:
   ```bash
   npm run type-check
   ```
   Expected: zero errors.

2. Unit tests:
   ```bash
   npm test -- --testPathPattern="chat" 2>&1 || npm test 2>&1
   ```
   Expected: all tests pass (no new failures).

3. Architecture validation:
   ```bash
   npm run check:architecture 2>&1
   ```
   Expected: no FSD violations (new chat-detail-panel.tsx is within the chat module boundary).

4. Export validation:
   ```bash
   npm run validate:exports 2>&1
   ```
   Expected: no barrier violations.

5. Grep checks to confirm preserved code:
   ```bash
   grep -n "UserDetailSheet" src/app/\(authenticated\)/chat/components/chat-window.tsx
   grep -n "lazy\|Suspense" src/app/\(authenticated\)/chat/components/chat-layout.tsx
   grep -n "ChatDetailPanel" src/app/\(authenticated\)/chat/components/chat-layout.tsx
   grep -n "toggleProfileSheet" src/app/\(authenticated\)/chat/components/chat-header.tsx
   ```

If any check fails, fix the issue before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>npm run type-check && npm run check:architecture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run type-check exits with code 0
    - npm run check:architecture shows no violations for chat module
    - grep confirms UserDetailSheet still imported in chat-window.tsx
    - grep confirms Suspense and lazy still present in chat-layout.tsx
    - grep confirms ChatDetailPanel present in chat-layout.tsx
    - grep confirms toggleProfileSheet present in chat-header.tsx
  </acceptance_criteria>
  <done>All automated checks pass; preserved code confirmed via grep</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human regression walkthrough</name>
  <what-built>
    Complete chat module redesign (Phases 01-04):
    - 3-column Glass Briefing layout with redesigned sidebar
    - Glassmorphic header with video/audio call buttons
    - Asymmetric message bubbles, date separators, media bubbles (file, audio waveform, image, video)
    - Auto-resize textarea input with glass styling, typing indicator
    - Context bar for conversations linked to a processo
    - Empty state with suggestion cards when no conversation is selected
    - Inline detail panel (xl+ screens) with avatar, status, info sections
    - All existing features preserved: real-time messaging, calls, file upload, audio recording
  </what-built>
  <how-to-verify>
    Run `npm run dev` and navigate to `/app/chat`. Test these flows:

    **Layout & Navigation**
    1. Open chat on desktop (xl+): verify 3-column layout — sidebar | chat area | (detail panel hidden)
    2. Open chat on mobile: verify sidebar is shown first, hides when conversation selected
    3. Select a conversation: sidebar collapses on mobile, chat area shows

    **Detail Panel**
    4. On xl+ screen: click the contact avatar/name in the chat header — verify detail panel opens in 3rd column
    5. Verify panel shows: avatar, name, online status, and any available info (phone, country, media)
    6. Click the X button in the panel — verify it closes
    7. On mobile: click avatar/name — verify UserDetailSheet opens as floating modal (not inline panel)

    **Messaging**
    8. Type a message and send — verify it appears as a sent bubble with correct styling
    9. Verify received messages show with asymmetric corners and sender info in groups
    10. Verify date separators appear between message groups from different days

    **Media & Files**
    11. Upload a file — verify FileChatBubble renders with download button
    12. Record an audio message — verify AudioChatBubble with waveform appears
    13. Send an image — verify ImageChatBubble with correct border-radius

    **Real-time & Presence**
    14. Open chat in a second browser tab as a different user and send a message — verify it arrives in real-time
    15. Verify typing indicator appears when remote user types

    **Calls**
    16. Click video call button — verify CallSetupDialog opens
    17. Click audio call button — verify CallSetupDialog opens with audio type

    **Context Bar & Empty State**
    18. Select a conversation linked to a processo — verify ChatContextBar appears below header
    19. Deselect all conversations (click back on mobile or switch to desktop with no selection) — verify ChatEmptyState appears with suggestion cards
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
All automated checks passed + human walkthrough confirmed zero regression across all 19 verification points.
</verification>

<success_criteria>
- npm run type-check: zero errors
- npm run check:architecture: no violations
- All 19 human verification flows pass without issues
- Phase 04 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-detail-panel-preservation/04-02-SUMMARY.md`
</output>
