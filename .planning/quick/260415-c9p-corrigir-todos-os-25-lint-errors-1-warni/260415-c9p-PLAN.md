---
phase: 260415-c9p-lint-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(authenticated)/assinatura-digital/components/formularios-glass-list.tsx
  - src/app/(authenticated)/assinatura-digital/components/templates-glass-list.tsx
  - src/app/(authenticated)/captura/advogados/page-client.tsx
  - src/app/(authenticated)/captura/agendamentos/page-client.tsx
  - src/app/(authenticated)/captura/components/captura-glass-cards.tsx
  - src/app/(authenticated)/captura/credenciais/page-client.tsx
  - src/app/(authenticated)/contratos/components/contrato-view-sheet.tsx
  - src/app/(authenticated)/contratos/components/contratos-table-wrapper.tsx
  - src/app/(authenticated)/entrevistas-trabalhistas/components/entrevista-resumo.tsx
  - src/app/(authenticated)/usuarios/components/detail/profile-sidebar.tsx
  - src/app/(authenticated)/usuarios/components/permissions/permission-toggle.tsx
  - src/components/auth/forgot-password-form.tsx
  - src/components/layout/header/header-user-menu.tsx
  - src/components/layout/sidebar/nav-user.tsx
autonomous: true
requirements:
  - LINT-01
must_haves:
  truths:
    - "npm run lint termina com exit code 0"
    - "Nenhum warning ou error reportado pelo ESLint"
    - "Nenhum token de design system hardcoded (OKLCH literal removido)"
    - "Nenhum <h2> direto em profile-sidebar (usa Typography.H2)"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Sombra usando oklch(from var(--token) ...) ao invés de OKLCH literal"
      contains: "oklch(from var("
    - path: "src/app/(authenticated)/usuarios/components/detail/profile-sidebar.tsx"
      provides: "Typography.H2 no lugar de <h2> direto"
  key_links:
    - from: "npm run lint"
      to: "eslint config (--max-warnings=0)"
      via: "package.json scripts"
      pattern: "lint.*max-warnings"
---

<objective>
Zerar todos os 25 errors + 1 warning reportados por `npm run lint`.

Purpose: Manter a base limpa para CI e preservar regras do design system (Glass Briefing) aplicadas via ESLint custom rules (no-restricted-syntax para OKLCH literal, enforcement de Typography.H2).

Output: 15 arquivos corrigidos. `npm run lint` passa com exit code 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

Referências do design system relevantes:
- Regra: nunca hardcode cores/OKLCH — usar tokens ou `oklch(from var(--token) l c h / alpha)`
- Regra: usar `Typography.H2` ou `className="typography-h2"` no lugar de `<h2>` direto

Validation: `npm run lint` tem `--max-warnings=0`, portanto o warning do contratos-table-wrapper também DEVE ser corrigido.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover imports e variáveis não usadas (17 errors triviais)</name>
  <files>
    src/app/(authenticated)/assinatura-digital/components/formularios-glass-list.tsx,
    src/app/(authenticated)/assinatura-digital/components/templates-glass-list.tsx,
    src/app/(authenticated)/captura/advogados/page-client.tsx,
    src/app/(authenticated)/captura/agendamentos/page-client.tsx,
    src/app/(authenticated)/captura/components/captura-glass-cards.tsx,
    src/app/(authenticated)/captura/credenciais/page-client.tsx,
    src/app/(authenticated)/contratos/components/contrato-view-sheet.tsx,
    src/app/(authenticated)/entrevistas-trabalhistas/components/entrevista-resumo.tsx,
    src/app/(authenticated)/usuarios/components/permissions/permission-toggle.tsx,
    src/components/auth/forgot-password-form.tsx,
    src/components/layout/header/header-user-menu.tsx,
    src/components/layout/sidebar/nav-user.tsx
  </files>
  <action>
    Remover imports e variáveis não usadas. Para cada arquivo:

    **Imports não usados — REMOVER a linha de import (ou o símbolo específico do import agrupado):**
    - `formularios-glass-list.tsx:20` → remover import `Skeleton`
    - `templates-glass-list.tsx:11` → remover `ChevronRight` do import de `lucide-react`
    - `templates-glass-list.tsx:19` → remover import `Skeleton`
    - `captura/agendamentos/page-client.tsx:24` → remover import `CapturaStatusSemanticBadge`
    - `captura/agendamentos/page-client.tsx:25` → remover import `Button`
    - `captura/components/captura-glass-cards.tsx:20` → remover import `Skeleton`
    - `contrato-view-sheet.tsx:20` → remover import `GlassPanel`
    - `entrevista-resumo.tsx:10` → remover import `Heading`
    - `forgot-password-form.tsx:7` → remover import `cn`

    **Variáveis atribuídas mas não usadas — INVESTIGAR antes de remover:**

    1. `captura/advogados/page-client.tsx:139` — `credenciaisMap`: abrir arquivo, ler contexto ao redor (linhas ~130-180). Se é dead code órfão, remover a declaração completa. Se parece wire-up pendente (ex: passed a um componente comentado), prefixar com `_credenciaisMap` para silenciar o lint mas preservar o código.

    2. `captura/credenciais/page-client.tsx:209` — `handleNovaCredencial`: mesmo critério. Se não é passado a nenhum handler (botão, callback), remover. Se existe referência em comentário/JSX comentado, prefixar com `_`.

    3. `header/header-user-menu.tsx:48` — `logout`: verificar se é do useAuth/hook. Se não é chamado, remover a destruturação (ou prefixar `_logout` se a assinatura do hook requer).

    4. `sidebar/nav-user.tsx:62` — `setMounted`: se `mounted` é usado mas `setMounted` nunca é chamado, é bug real. Verificar se falta um `useEffect(() => setMounted(true), [])`. Se for o caso de SSR mount pattern (mounted sempre false), simplesmente remover `mounted` e `setMounted` e a lógica que depende. Documentar no commit qual caminho foi tomado.

    **Parâmetros não usados (prop signature precisa existir):**
    - `permission-toggle.tsx:15` — renomear parâmetro `operacao` para `_operacao` (convenção unused-imports: prefixo `_` silencia o lint para args de callback/handler).

    Para cada edição, verificar que nenhuma referência ao símbolo removido permanece no arquivo (busca de texto). Não alterar exports públicos.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "(no-unused-vars|no-unused-imports)" | wc -l</automated>
    Esperado: 0
  </verify>
  <done>
    Todos os 17 errors de unused-imports/unused-vars eliminados. Comportamento runtime preservado (nenhuma remoção acidental de código vivo — dead code genuíno removido, stubs pendentes prefixados com `_`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir violações de design system e hook deps (4 errors + 1 warning)</name>
  <files>
    src/app/(auth)/layout.tsx,
    src/app/(authenticated)/usuarios/components/detail/profile-sidebar.tsx,
    src/app/(authenticated)/contratos/components/contratos-table-wrapper.tsx
  </files>
  <action>
    **Fix 1 — OKLCH literal em `(auth)/layout.tsx:32`:**

    Atual (linha 32):
    ```
    shadow-[0_8px_32px_oklch(0_0_0/0.25)]
    ```

    A sombra usa preto literal em OKLCH. Per CLAUDE.md: "Nunca hardcode cores — Use CSS variables ou `oklch(from var(--token) l c h / alpha)` para opacidade."

    Substituir por sombra derivada do token `--shadow-color` (ou `--foreground` como fallback se `--shadow-color` não existir no globals.css). Preferência de ordem:
    1. Se `--shadow-color` existe em `src/app/globals.css` → usar `shadow-[0_8px_32px_oklch(from_var(--shadow-color)_l_c_h/0.25)]`
    2. Senão, usar `shadow-[0_8px_32px_oklch(from_var(--foreground)_0_0_h/0.25)]` (l=0 força preto mantendo sintaxe compatível)

    Verificar `src/app/globals.css` (grep por `--shadow`) antes de decidir. A classe final deve ter underscores entre tokens (Tailwind arbitrary values) OU usar sintaxe com aspas: `shadow-[0_8px_32px_oklch(from_var(--foreground)_0_0_0/0.25)]`.

    **Fix 2 — `<h2>` direto em `profile-sidebar.tsx:121`:**

    Ler contexto da linha 121 e substituir `<h2 ...>` por `<Typography.H2 ...>` preservando className e children. Adicionar import `import { Typography } from "@/components/ui/typography"` se ainda não existir (verificar se o componente é exportado assim — conferir em `src/components/ui/typography.tsx`; se a API for `Heading level="h2"`, usar essa forma. Decisão: seguir o que a lint rule sugere na mensagem — `Typography.H2` OU `className="typography-h2"` em `<h2>`). Mais seguro: manter `<h2>` mas adicionar `className="typography-h2"` mesclado com classes existentes via `cn()`.

    **Fix 3 — useCallback missing dep em `contratos-table-wrapper.tsx:423`:**

    Adicionar `setCreateOpen` ao array de dependências do `React.useCallback` na linha ~423. Como `setCreateOpen` é um setter de `useState`, é estável — pode ser adicionado sem risco de re-criação desnecessária.

    Se o setter vier de `useState` local do componente, o fix é literal: `[...deps, setCreateOpen]`.
  </action>
  <verify>
    <automated>npm run lint</automated>
    Esperado: exit code 0, zero errors, zero warnings.
  </verify>
  <done>
    `npm run lint` passa com `--max-warnings=0`. OKLCH literal substituído por token. `<h2>` alinhado ao design system. Hook deps corretas.
  </done>
</task>

</tasks>

<verification>
Comando único decisivo:
```bash
npm run lint
```
Exit code DEVE ser 0. Saída DEVE terminar sem linhas `error` ou `warning`.

Checks auxiliares (opcionais, rapidez):
```bash
npm run lint 2>&1 | tail -20
git diff --stat
```

Confirmar que nenhum arquivo fora de `files_modified` foi tocado.
</verification>

<success_criteria>
- [ ] `npm run lint` exit code 0
- [ ] Zero errors, zero warnings reportados
- [ ] `src/app/(auth)/layout.tsx` não contém literal `oklch(0 0 0/...)` — usa `oklch(from var(...))`
- [ ] `profile-sidebar.tsx` não tem `<h2>` direto sem classe typography
- [ ] `contratos-table-wrapper.tsx:423` useCallback inclui `setCreateOpen` nas deps
- [ ] Nenhum comportamento runtime regressado (imports removidos eram realmente mortos)
- [ ] Decisões de remoção vs. prefixo `_` documentadas no commit message para `credenciaisMap`, `handleNovaCredencial`, `logout`, `setMounted`
</success_criteria>

<output>
After completion, create `.planning/quick/260415-c9p-corrigir-todos-os-25-lint-errors-1-warni/260415-c9p-SUMMARY.md`
</output>
