---
quick_task: 260414-e75
subsystem: expedientes
tags: [glass-briefing, refactor, filter-bar, ui, dead-code-cleanup]
tech_stack:
  patterns: [glass-dropdown, Popover, FilterDropdownTrigger, SimpleFilter]
key_files:
  created:
    - src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx
  modified:
    - src/app/(authenticated)/expedientes/components/expedientes-content.tsx
    - src/app/(authenticated)/expedientes/components/expedientes-list-wrapper.tsx
    - src/app/(authenticated)/expedientes/components/expedientes-glass-list.tsx
  deleted:
    - src/app/(authenticated)/expedientes/components/expediente-list-row.tsx
decisions:
  - Kept expedientes-list-filters.tsx (not deleted as plan stated) because expedientes-table-wrapper.tsx still imports it
  - Named filter interface ExpedientesFilterBarFilters to avoid conflict with existing ExpedientesFilters type in domain.ts
metrics:
  duration: "9min"
  completed_date: "2026-04-14"
  tasks: 3
  files_changed: 4
---

# Quick Task 260414-e75: Refatorar Expedientes Glass Briefing Summary

Refatorou listagem de expedientes com nova filter bar glass-dropdown, simplificou wrapper removendo modo tabela, enriqueceu rows glass com badges inline de indicadores (segredo, digital, prioridade, tipo) e corrigiu botao de header para usar Button shared.

## What Was Built

### Task 1 — Filter bar + simplify wrapper + fix header + cleanup (commit `00c850b15`)

- Criado `ExpedientesFilterBar` com 5 filtros glass-dropdown (Tribunal, Grau, Origem, Responsavel, Tipo) seguindo EXATAMENTE o padrao de `audiencias-filter-bar.tsx` (FilterDropdownTrigger, Popover com `glass-dropdown`, active/inactive states).
- Substituido `<button>` raw do header por `Button` shared de `@/components/ui/button`.
- Simplificado `ExpedientesListWrapper`: removido modo tabela (DataShell, DataTable, DataTableToolbar, columns, listMode toggle, density, tableMeta). Agora aceita `filters` via props ao inves de estado interno.
- Deletado `expediente-list-row.tsx` (dead code confirmado - zero referencias no codebase).
- `expedientes-content.tsx` agora gerencia state de filtros e passa para o wrapper.

### Task 2 — Enrich glass list rows (commit `6619916e6`)

- `ExpedientesGlassList` agora aceita `tiposExpedientesData` prop.
- Main cell das rows agora mostra info empilhada (como audiencias):
  - Titulo: numero processo + badge de tipo expediente + badges indicadores (Segredo, Digital, Prioridade, Vencido)
  - Partes: autora vs re
  - Classe judicial
  - Orgao julgador (via `descricaoOrgaoJulgador || orgaoJulgadorOrigem`)
- Botao "Baixar" agora sempre visivel (removido `opacity-0 group-hover:opacity-100`).
- Icons adicionados: `Lock`, `Monitor`, `AlertTriangle` de lucide-react.

### Task 3 — Type-check verification

- `npm run type-check` passou sem erros. Zero regressoes TypeScript.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] Kept expedientes-list-filters.tsx (plan said delete)**
- **Found during:** Task 1
- **Issue:** Plan instructed to delete `expedientes-list-filters.tsx`, but `expedientes-table-wrapper.tsx` (also exported from `index.ts` barrel) actively imports `ExpedientesListFilters` at line 39 and uses it at line 401.
- **Fix:** Restored the file with original content. The table wrapper remains functional; the glass view wrapper no longer uses these multi-select filters (replaced by the new single-select `ExpedientesFilterBar`).
- **Files kept:** `src/app/(authenticated)/expedientes/components/expedientes-list-filters.tsx`
- **Rationale:** Deleting the file would have broken the table wrapper and barrel export. Both patterns (old table view with multi-filter, new glass view with single-filter bar) now coexist.

**2. [Rule 3 - Blocker] Renamed filter interface to avoid type collision**
- **Found during:** Task 1
- **Issue:** Plan named the new interface `ExpedientesFilters`, but `domain.ts` already exports an `ExpedientesFilters` type (`Omit<ListarExpedientesParams, ...>`).
- **Fix:** Named the new interface `ExpedientesFilterBarFilters` to avoid conflict while keeping existing domain type intact.
- **Files affected:** `expedientes-filter-bar.tsx`, `expedientes-content.tsx`, `expedientes-list-wrapper.tsx`

**3. [Rule 1 - Bug] Used correct field for orgao julgador**
- **Found during:** Task 2
- **Issue:** Plan referenced `expediente.orgaoJulgador`, but `Expediente` type has `descricaoOrgaoJulgador` and `orgaoJulgadorOrigem` (no `orgaoJulgador`).
- **Fix:** Used `expediente.descricaoOrgaoJulgador || expediente.orgaoJulgadorOrigem` matching the legacy pattern from `expediente-list-row.tsx`.
- **Files affected:** `expedientes-glass-list.tsx`

## Self-Check: PASSED

**Created files verified:**
- FOUND: src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx

**Commits verified:**
- FOUND: 00c850b15 — Task 1 (filter bar + simplify wrapper + header button)
- FOUND: 6619916e6 — Task 2 (enrich glass list rows)

**Type-check:** PASSED (zero errors from `npm run type-check`)

**Deleted files confirmed gone:**
- expediente-list-row.tsx — removed, no references remain
