---
phase: quick
plan: 260414-e75
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(authenticated)/expedientes/components/expedientes-list-wrapper.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-glass-list.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-content.tsx
  - src/app/(authenticated)/expedientes/components/expediente-list-row.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-list-filters.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Lista de expedientes mostra apenas glass rows (sem toggle table/glass)"
    - "Glass rows exibem tipo expediente badge, orgao julgador, classe judicial, e indicator badges inline"
    - "Filter bar usa chips Glass Briefing com popovers glass-dropdown (sem Command/FilterPopoverMulti pattern)"
    - "Botao Novo Expediente usa componente Button shadcn com rounded-xl"
    - "Dead code removido (expediente-list-row.tsx, expedientes-list-filters.tsx)"
  artifacts:
    - path: "src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx"
      provides: "Glass Briefing chip filter bar para expedientes"
    - path: "src/app/(authenticated)/expedientes/components/expedientes-list-wrapper.tsx"
      provides: "Simplified wrapper: glass list + pagination only"
    - path: "src/app/(authenticated)/expedientes/components/expedientes-glass-list.tsx"
      provides: "Enriched glass rows with tipo, orgao, classe, indicator badges"
  key_links:
    - from: "expedientes-content.tsx"
      to: "expedientes-filter-bar.tsx"
      via: "filters state + onChange prop"
      pattern: "ExpedientesFilterBar.*filters.*onChange"
    - from: "expedientes-content.tsx"
      to: "expedientes-list-wrapper.tsx"
      via: "filters prop passthrough"
      pattern: "ExpedientesListWrapper.*filters"
---

<objective>
Refatorar o modulo expedientes para alinhar completamente ao design system Glass Briefing, removendo a view de tabela legada, enriquecendo as glass rows, e migrando os filtros para o padrao chip-based usado em audiencias.

Purpose: Consistencia visual com audiencias e demais modulos ja migrados para Glass Briefing.
Output: Expedientes com glass list unica, filter bar chip-based, rows enriquecidas, dead code removido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(authenticated)/expedientes/components/expedientes-content.tsx
@src/app/(authenticated)/expedientes/components/expedientes-list-wrapper.tsx
@src/app/(authenticated)/expedientes/components/expedientes-glass-list.tsx
@src/app/(authenticated)/expedientes/components/expedientes-list-filters.tsx
@src/app/(authenticated)/expedientes/components/expediente-list-row.tsx
@src/app/(authenticated)/expedientes/domain.ts
@src/app/(authenticated)/audiencias/components/audiencias-filter-bar.tsx
@src/app/(authenticated)/audiencias/components/audiencias-glass-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filter bar + simplify wrapper + fix header + cleanup dead code</name>
  <files>
    src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx
    src/app/(authenticated)/expedientes/components/expedientes-list-wrapper.tsx
    src/app/(authenticated)/expedientes/components/expedientes-content.tsx
    src/app/(authenticated)/expedientes/components/expediente-list-row.tsx
    src/app/(authenticated)/expedientes/components/expedientes-list-filters.tsx
  </files>
  <action>
**1a. Create `expedientes-filter-bar.tsx`** — new file following the audiencias-filter-bar.tsx pattern exactly:

- Export `ExpedientesFilters` interface:
  ```ts
  export interface ExpedientesFilters {
    trt: string[];
    grau: string | null;
    origem: string | null;
    responsavel: number | 'null' | null;
    tipo: number | null;
  }
  ```
- Export `EMPTY_FILTERS` constant with all null/empty defaults.
- Reuse the SAME internal patterns from audiencias-filter-bar.tsx:
  - `FilterDropdownTrigger` component with `border-primary/20 bg-primary/5 text-primary` active state, `border-border/15 text-muted-foreground/60 hover:bg-muted/30` inactive.
  - `POPOVER_CLASSES = 'rounded-2xl glass-dropdown overflow-hidden p-0'` on PopoverContent.
  - Simple button list inside popovers for Grau (3 static options from GRAU_TRIBUNAL_LABELS), Origem (3 static from ORIGEM_EXPEDIENTE_LABELS), Tipo (dynamic from tiposExpedientes prop).
  - Command-based searchable popover for Tribunal (25 TRT options — same list from audiencias) and Responsavel (dynamic from usuarios prop, with "Sem responsavel" option).
  - Each filter chip shows X to clear when active.
  - ChevronDown icon that rotates 180 when open.
- 5 filters total: Tribunal (multi-select), Grau (single), Origem (single), Responsavel (single with search), Tipo (single).
- NO boolean filters (juizoDigital, segredoJustica, prioridadeProcessual) — those are visible via indicator badges in glass rows.
- Props: `filters: ExpedientesFilters`, `onChange: (f: ExpedientesFilters) => void`, `usuarios: Usuario[]`, `tiposExpedientes: TipoExpediente[]`.

**1b. Simplify `expedientes-list-wrapper.tsx`:**

- Remove ALL: `listMode` state, `listModeToggle` JSX, `table` state, `density` state, `columns` import, `tableMeta` useMemo, `DataTable`/`DataShell`/`DataTableToolbar` imports, `ExpedientesListFilters` import, `cn` import (if unused).
- Remove ALL filter state variables (trtFiltro, grauFiltro, origemFiltro, responsavelFiltro, tipoExpedienteFiltro, juizoDigitalFiltro, segredoJusticaFiltro, prioridadeProcessualFiltro) and withPageReset helper.
- Accept new prop `filters: ExpedientesFilters` (imported from filter-bar).
- Map `filters` to useExpedientes params: `trt: filters.trt.length === 1 ? filters.trt[0] : undefined`, `grau: filters.grau || undefined`, `origem: filters.origem || undefined`, `responsavelId: filters.responsavel === 'null' ? 'null' : filters.responsavel || undefined`, `tipoExpedienteId: filters.tipo || undefined`. No boolean filter params.
- Keep: pagination state (pageIndex, pageSize), useExpedientes hook, DataPagination, ExpedientesGlassList, useUsuarios, useTiposExpedientes.
- Pass `tiposExpedientesData={tiposExpedientes}` to ExpedientesGlassList (new prop for Task 2).
- Final render: `<div className="space-y-4"><ExpedientesGlassList .../><DataPagination .../></div>` — no DataShell wrapper.

**1c. Update `expedientes-content.tsx`:**

- Import `ExpedientesFilterBar`, `ExpedientesFilters`, `EMPTY_FILTERS` from `./expedientes-filter-bar`.
- Add state: `const [filters, setFilters] = useState<ExpedientesFilters>(EMPTY_FILTERS)`.
- Replace raw `<button>` (lines 273-279) with `<Button size="sm" className="rounded-xl"><Plus className="size-3.5" />Novo Expediente</Button>`, using the existing Button import.
- Add `<ExpedientesFilterBar filters={filters} onChange={setFilters} usuarios={usuarios || []} tiposExpedientes={tiposExpedientes || []} />` between TabPills and the SearchInput/ViewToggle div in the View Controls section (section 4). Wrap TabPills and FilterBar together on the left, keep SearchInput+ViewToggle on the right.
- Pass `filters={filters}` to `<ExpedientesListWrapper>`.

**1d. Delete dead code:**

- DELETE `expediente-list-row.tsx` entirely (confirmed zero imports across codebase).
- DELETE `expedientes-list-filters.tsx` entirely (no longer imported after wrapper refactor).
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | grep -E "expedientes/" | head -20; echo "---"; grep -r "expediente-list-row\|expedientes-list-filters" src/app/\(authenticated\)/expedientes/ --include="*.tsx" --include="*.ts" | grep -v "\.bak" | head -10</automated>
  </verify>
  <done>
    - expedientes-filter-bar.tsx exists with Glass Briefing chip pattern (5 filters)
    - expedientes-list-wrapper.tsx has no DataTable/DataShell/toggle references, accepts filters prop
    - expedientes-content.tsx has filter state, renders ExpedientesFilterBar, uses Button component for "Novo Expediente"
    - expediente-list-row.tsx and expedientes-list-filters.tsx deleted, zero dangling imports
    - TypeScript compiles without errors in expedientes/
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich glass list rows with tipo badge, orgao, classe, indicator badges</name>
  <files>
    src/app/(authenticated)/expedientes/components/expedientes-glass-list.tsx
  </files>
  <action>
Update ExpedientesGlassList to show enriched data per row, following audiencias-glass-list.tsx GlassRow pattern.

**Props changes:**
- Add `tiposExpedientesData?: TipoExpediente[]` to `ExpedientesGlassListProps` (where TipoExpediente = `{ id: number; tipoExpediente?: string; tipo_expediente?: string }`).
- Pass through to GlassRow.

**GlassRow enrichments — update the main cell (column 2, "Processo / Partes"):**

After the processo number line, add stacked info:
1. **Tipo expediente badge** — inline after numero processo in same flex row. Resolve label from `tiposExpedientesData` by matching `expediente.tipoExpedienteId`. Style: `bg-primary/8 text-primary text-[9px] font-medium px-1.5 py-0.5 rounded-md`. If no tipo, skip.
2. **Partes line** — already exists, keep as is.
3. **Orgao julgador + Classe judicial** — new line below partes. Show `expediente.orgaoJulgadorOrigem` (text-[10px] text-muted-foreground/50) separated by ` · ` from `expediente.classeJudicial` if both exist. If only one exists show just that one. If neither, skip line.
4. **Indicator badges inline** — new flex row below orgao/classe. Only show badges that apply:
   - Segredo Justica (`expediente.segredoJustica === true`): `<Lock className="size-2.5" />` with `bg-warning/10 border border-warning/20 text-warning rounded px-1.5 py-0.5 text-[9px] font-medium inline-flex items-center gap-1`
   - Juizo Digital (`expediente.juizoDigital === true`): `<Monitor className="size-2.5" />` with `bg-info/10 border border-info/25 text-info` same badge pattern
   - Prioridade Processual (`expediente.prioridadeProcessual === true`): `<AlertTriangle className="size-2.5" />` with `bg-destructive/10 border border-destructive/20 text-destructive` same badge pattern
   - If no indicators apply, skip entire row.

**Actions column (column 8) — make "Baixar" always visible:**
- Remove `opacity-0 group-hover:opacity-100` from the Baixar button. Keep it always visible.
- Keep ChevronRight with `opacity-0 group-hover:opacity-100` transition (subtle directional hint is fine on hover).

**Grid template update:**
- Keep the same grid: `grid-cols-[32px_2.5fr_1fr_0.8fr_0.8fr_80px_80px_40px]`. The main cell (2.5fr) has room for stacked info. Widening the action column to `56px` to fit always-visible Baixar: `grid-cols-[32px_2.5fr_1fr_0.8fr_0.8fr_80px_80px_56px]`.
- Update ColumnHeaders and ListSkeleton to match the same grid if column widths change.

**New imports needed:** `Lock`, `Monitor`, `AlertTriangle` from lucide-react. Import `TipoExpediente` type (define locally or import from appropriate source).
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | grep -E "expedientes/" | head -20; echo "---"; grep -c "Lock\|Monitor\|AlertTriangle\|segredoJustica\|juizoDigital\|prioridadeProcessual\|tiposExpedientesData\|orgaoJulgadorOrigem\|classeJudicial" src/app/\(authenticated\)/expedientes/components/expedientes-glass-list.tsx</automated>
  </verify>
  <done>
    - GlassRow shows tipo expediente badge inline with processo number
    - GlassRow shows orgao julgador and classe judicial below partes
    - GlassRow shows conditional indicator badges (Segredo, Juizo Digital, Prioridade) with correct colors and icons
    - Baixar button always visible (no opacity-0)
    - tiposExpedientesData prop accepted and used
    - TypeScript compiles clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Refatoracao completa dos expedientes para Glass Briefing:
    1. Filter bar chip-based (Tribunal, Grau, Origem, Responsavel, Tipo) substituindo filtros antigos
    2. Glass rows enriquecidas com tipo badge, orgao julgador, classe judicial, indicator badges (Segredo, Juizo Digital, Prioridade)
    3. Wrapper simplificado (sem toggle table/glass, sem DataShell)
    4. Header com Button component, dead code removido
  </what-built>
  <how-to-verify>
    1. Abra http://localhost:3000/app/expedientes/lista
    2. Confirme que NAO existe toggle "Rows/Tabela" — apenas glass rows
    3. Verifique que glass rows mostram: tipo expediente badge (cor primaria), orgao julgador, classe judicial, badges de Segredo/Juizo Digital/Prioridade quando aplicavel
    4. Verifique que botao "Baixar" esta sempre visivel (nao apenas no hover)
    5. Clique nos filtros chip acima da lista — confirme estilo Glass Briefing (chips com borda, popovers glass-dropdown, X para limpar)
    6. Aplique um filtro (ex: Tribunal TRT2) e confirme que a lista filtra corretamente
    7. Volte para /app/expedientes (quadro) e confirme que botao "Novo Expediente" usa estilo rounded-xl consistente
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros no modulo expedientes
- Nenhuma referencia a expediente-list-row ou expedientes-list-filters restante
- Filter bar renderiza 5 chips com glass-dropdown popovers
- Glass rows mostram informacao enriquecida (tipo, orgao, classe, indicators)
</verification>

<success_criteria>
- View lista mostra apenas glass rows com dados enriquecidos (tipo, orgao, classe, indicators)
- Filter bar segue padrao Glass Briefing identico ao de audiencias
- Botao "Novo Expediente" usa Button component
- Zero dead code (expediente-list-row.tsx, expedientes-list-filters.tsx removidos)
- TypeScript compila limpo
</success_criteria>

<output>
After completion, create `.planning/quick/260414-e75-refatorar-expedientes-glass-briefing-rem/260414-e75-SUMMARY.md`
</output>
