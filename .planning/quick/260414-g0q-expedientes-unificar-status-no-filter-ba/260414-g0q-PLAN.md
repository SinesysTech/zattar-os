---
phase: 260414-g0q-expedientes-unificar-status-no-filter-ba
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-content.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-month-wrapper.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-calendar-compact.tsx
  - src/app/(authenticated)/expedientes/components/expedientes-day-list.tsx
autonomous: true
requirements: [QUICK-G0Q-01, QUICK-G0Q-02]
must_haves:
  truths:
    - "Status filter (Pendentes/Baixados/Todos) lives INSIDE ExpedientesFilterBar as first dropdown with counts inline"
    - "TabPills are removed from expedientes view controls — status is no longer a separate tab row"
    - "Status defaults to 'pendentes' and its trigger highlights (active) only when value is not 'pendentes'"
    - "Month view uses a single-panel layout (no 400px sidebar) following audiencias-glass-month.tsx pattern"
    - "Month view shows weekday headers (Monday-start), 7×6 grid with dots/badges per day, and a summary strip"
    - "Dead files expedientes-calendar-compact.tsx and expedientes-day-list.tsx are deleted"
    - "All existing expedientes filtering behavior (trt, grau, origem, responsavel, tipo) continues to work"
  artifacts:
    - path: "src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx"
      provides: "StatusFilter sub-component, updated ExpedientesFilterBarFilters interface with status field, counts prop"
      contains: "StatusFilter"
    - path: "src/app/(authenticated)/expedientes/components/expedientes-content.tsx"
      provides: "Wired filters.status (no activeTab, no TabPills), passes counts to filter bar"
      contains: "status: 'pendentes'"
    - path: "src/app/(authenticated)/expedientes/components/expedientes-month-wrapper.tsx"
      provides: "Single-panel month view mirroring audiencias-glass-month.tsx"
      contains: "grid-cols-7"
  key_links:
    - from: "expedientes-content.tsx"
      to: "ExpedientesFilterBar"
      via: "filters + counts props"
      pattern: "counts=\\{globalCounts\\}"
    - from: "expedientes-content.tsx"
      to: "ExpedientesListWrapper"
      via: "activeTab={filters.status}"
      pattern: "activeTab=\\{filters\\.status\\}"
    - from: "expedientes-month-wrapper.tsx"
      to: "getExpedienteUrgencyLevel + URGENCY_DOT"
      via: "urgency-helpers import"
      pattern: "getExpedienteUrgencyLevel"
---

<objective>
Unify status control (Pendentes/Baixados/Todos) into the ExpedientesFilterBar as a first dropdown with inline counts (eliminating the separate TabPills row), and rewrite the month view wrapper to mirror the clean single-panel Audiencias Glass Month layout — deleting the now-dead compact calendar and day list files.

Purpose: Visual and interaction consistency with Audiencias module (already shipped in 260414-e75); simpler month view that matches the established Glass Briefing pattern.
Output: Updated filter bar + content orchestrator + rewritten month wrapper, two dead files removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# Reference implementation — COPY THIS PATTERN
@src/app/(authenticated)/audiencias/components/audiencias-filter-bar.tsx
@src/app/(authenticated)/audiencias/components/audiencias-glass-month.tsx

# Files being modified
@src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx
@src/app/(authenticated)/expedientes/components/expedientes-content.tsx
@src/app/(authenticated)/expedientes/components/expedientes-month-wrapper.tsx

# Helpers used by month view
@src/app/(authenticated)/expedientes/components/urgency-helpers.ts

# Files being deleted (read to confirm no external references)
@src/app/(authenticated)/expedientes/components/expedientes-calendar-compact.tsx
@src/app/(authenticated)/expedientes/components/expedientes-day-list.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integrate StatusFilter into ExpedientesFilterBar and rewire expedientes-content</name>
  <files>
    src/app/(authenticated)/expedientes/components/expedientes-filter-bar.tsx,
    src/app/(authenticated)/expedientes/components/expedientes-content.tsx
  </files>
  <action>
    **Step A — Update ExpedientesFilterBar:**

    1. Read `src/app/(authenticated)/audiencias/components/audiencias-filter-bar.tsx` lines 154-213 to understand the StatusFilter / FilterDropdown pattern used in audiencias (counts shown inline via `tabular-nums opacity-50 ml-auto`).

    2. In `expedientes-filter-bar.tsx`, update the exported `ExpedientesFilterBarFilters` interface to add `status: 'pendentes' | 'baixados' | 'todos'` as the first field (preserve all existing fields: trt, grau, origem, responsavel, tipo).

    3. Update `ExpedientesFilterBarProps` to include `counts: { pendentes: number; baixados: number; todos: number }`.

    4. Create a new `StatusFilter` sub-component that uses the existing `FilterDropdown` pattern of this file (same popover/trigger primitives). The trigger:
       - Label = `STATUS_LABELS[filters.status]` (define constant: `{ pendentes: 'Pendentes', baixados: 'Baixados', todos: 'Todos' }`)
       - `active={filters.status !== 'pendentes'}` (highlighted only when non-default)
       - NO `onClear` — status is always required (never clearable)
       - Popover options list: `[{ value: 'pendentes', label: 'Pendentes' }, { value: 'baixados', label: 'Baixados' }, { value: 'todos', label: 'Todos' }]`
       - Each option renders the label PLUS `<span className="text-[9px] ml-auto tabular-nums opacity-50">{counts[opt.value]}</span>`

    5. Render `<StatusFilter />` as the FIRST filter (before TRT/Grau/Origem/Responsavel/Tipo).

    **Step B — Update expedientes-content.tsx:**

    1. Remove `TabPills` import (line 14) and its rendering in the view-controls JSX block.
    2. Remove the `activeTab` useState (line 95) entirely.
    3. Remove the `tabs` useMemo (lines 215-219).
    4. Update `filters` useState initial value to include `status: 'pendentes'`:
       ```tsx
       const [filters, setFilters] = useState<ExpedientesFilterBarFilters>({
         status: 'pendentes', trt: null, grau: null, origem: null, responsavel: null, tipo: null,
       });
       ```
    5. Derive `baixadoFiltroAtivo` from `filters.status`:
       ```tsx
       const baixadoFiltroAtivo = useMemo(() => {
         if (filters.status === 'pendentes') return false;
         if (filters.status === 'baixados') return true;
         return undefined;
       }, [filters.status]);
       ```
    6. Replace ALL remaining `activeTab` references with `filters.status`. Notably:
       - `activeTab !== 'baixados'` → `filters.status !== 'baixados'`
       - Remove any `setActiveTab(...)` calls.
       - Pass to `<ExpedientesListWrapper activeTab={filters.status} ... />` (prop already accepts this shape).
    7. Pass counts to the filter bar:
       ```tsx
       <ExpedientesFilterBar
         filters={filters}
         onChange={setFilters}
         usuarios={usuarios || []}
         tiposExpedientes={tiposExpedientes || []}
         counts={globalCounts}
       />
       ```

    **Constraints:**
    - Use existing Glass Briefing tokens — no hardcoded colors.
    - No cross-deep imports: reuse patterns already in the file; do NOT import from audiencias internals.
    - Preserve all existing filter behavior (trt/grau/origem/responsavel/tipo unchanged).
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run check:architecture</automated>
  </verify>
  <done>
    - `npm run type-check` passes with zero new errors in `expedientes-filter-bar.tsx` / `expedientes-content.tsx`.
    - `TabPills` import/render no longer present in `expedientes-content.tsx` (grep confirms).
    - `activeTab` identifier no longer present in `expedientes-content.tsx` (grep confirms).
    - `ExpedientesFilterBarFilters` contains `status` field; `ExpedientesFilterBarProps` contains `counts`.
    - Status filter renders first with inline count labels and NO clear (X) button.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite expedientes-month-wrapper following audiencias-glass-month pattern and delete dead files</name>
  <files>
    src/app/(authenticated)/expedientes/components/expedientes-month-wrapper.tsx,
    src/app/(authenticated)/expedientes/components/expedientes-calendar-compact.tsx,
    src/app/(authenticated)/expedientes/components/expedientes-day-list.tsx
  </files>
  <action>
    **Step A — Full rewrite of expedientes-month-wrapper.tsx:**

    1. Read `src/app/(authenticated)/audiencias/components/audiencias-glass-month.tsx` lines 239-457 to understand the canonical month layout. Mirror its structure exactly (state shape, navigation buttons, DayCell, popover, summary strip).

    2. Replace current wrapper contents with a single `GlassPanel depth={1}` container using `className="p-6 flex-1 min-w-0 flex flex-col"`. Explicitly:
       - DO NOT use `grid-cols-[1fr_400px]` sidebar layout.
       - DO NOT use fixed `h-175` — rely on `flex-1` / natural flow.

    3. Component structure (top to bottom):
       - **Header row:** Month label (`format(currentMonth, 'MMMM yyyy', { locale: ptBR })`) + nav buttons (prev / today / next) using existing Button primitives. Pattern from audiencias.
       - **Legend row:** Small chips mapping the expedientes urgency scale (use keys from `URGENCY_DOT` in `./urgency-helpers.ts`). Replace audiencias' `Marcada/Finalizada/Cancelada` labels with the expedientes urgency labels.
       - **Weekday headers:** Monday-start — compute via `startOfWeek(monthStart, { weekStartsOn: 1 })`. Render `['Seg','Ter','Qua','Qui','Sex','Sab','Dom']` uppercase muted.
       - **Grid:** `grid grid-cols-7 gap-1.5` with `style={{ gridAutoRows: '1fr' }}`, 6 rows of 7 cells (42 days).
       - **DayCell** (internal component):
         - `min-h-[100px] sm:min-h-[120px]`
         - Today: rounded-full bg-primary chip on the day number
         - Weekend: muted bg token
         - Out-of-month: reduced opacity from `OPACITY_SCALE`
         - `< 3` items: colored dots (`URGENCY_DOT[urgency]`)
         - `>= 3` items: single count badge
         - Click → opens Popover anchored to that cell with the day's expedientes list
       - **Summary strip (bottom):** `total`, `vencidos`, `hoje`, `proximos` — computed with date-fns (`isBefore`, `isToday`, `isAfter`).

    4. Group expedientes by day using `parseISO(expediente.dataPrazoLegalParte)` (NOT audiencias' `dataInicio`).

    5. Use `getExpedienteUrgencyLevel(expediente)` from `./urgency-helpers.ts` to color dots and urgency badges. Use `URGENCY_DOT[urgency]` class map.

    6. Day's item rendering inside the popover should surface: tipoExpediente name, numeroProcesso, partes (autora vs re), orgaoJulgador. Open the detail dialog via `onViewDetail` on click.

    7. Props interface (exported):
       ```tsx
       export interface ExpedientesMonthWrapperProps {
         expedientes: Expediente[];
         onViewDetail?: (expediente: Expediente) => void;
       }
       ```

    8. Internal state (all `useState` inside the wrapper):
       - `currentMonth: Date` (default: `startOfMonth(new Date())`)
       - `openDayKey: string | null` (for popover control)

    **Step B — Delete dead files:**

    Before deleting, confirm no other files reference them:
    ```bash
    grep -rn "expedientes-calendar-compact\|expedientes-day-list" src/ --include="*.ts" --include="*.tsx"
    ```
    Only `expedientes-month-wrapper.tsx` should appear. After the rewrite removes those imports, delete:
    - `src/app/(authenticated)/expedientes/components/expedientes-calendar-compact.tsx`
    - `src/app/(authenticated)/expedientes/components/expedientes-day-list.tsx`

    **Constraints:**
    - Use Glass Briefing tokens only (no hex / bg-blue-*).
    - Use `Heading` / `Text` typography primitives from `@/components/ui/typography`.
    - Use `SPACING` / `SEMANTIC_SPACING` from `@/lib/design-system/tokens` — no arbitrary spacing.
    - Dark mode must work out of the box (CSS variables).
    - No cross-deep imports from audiencias — copy the pattern, do not import from it.
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run check:architecture &amp;&amp; npm run validate:exports</automated>
  </verify>
  <done>
    - `expedientes-month-wrapper.tsx` mirrors audiencias-glass-month structure (single GlassPanel, 7×6 grid, summary strip).
    - `expedientes-calendar-compact.tsx` and `expedientes-day-list.tsx` no longer exist.
    - `grep -rn "expedientes-calendar-compact\|expedientes-day-list" src/` returns zero matches.
    - Month view uses `getExpedienteUrgencyLevel` + `URGENCY_DOT` for coloring.
    - Groups items via `parseISO(expediente.dataPrazoLegalParte)`.
    - `npm run type-check` and `npm run check:architecture` pass with no new errors.
  </done>
</task>

</tasks>

<verification>
Run the full quick-check suite:
```bash
npm run type-check
npm run check:architecture
npm run validate:exports
```

Manual sanity checks (optional, visual):
- `/expedientes` route renders without runtime errors.
- Filter bar shows Status dropdown FIRST, with inline counts, no X button on status.
- Switching Status rotates between Pendentes/Baixados/Todos and filters the list accordingly.
- Month view is a single panel (no 400px sidebar), weekday headers Monday-start, dots/badges per day, summary strip at bottom.
- Dark mode renders correctly on both views.
</verification>

<success_criteria>
- Zero TypeScript errors introduced.
- Zero FSD architecture violations.
- TabPills and activeTab state fully removed from expedientes-content.tsx.
- Status filter is part of ExpedientesFilterBar, always-present, non-clearable, with inline counts.
- Month view matches audiencias-glass-month.tsx structure (adapted to expedientes data/urgency).
- Dead files removed; no dangling references anywhere in `src/`.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-g0q-expedientes-unificar-status-no-filter-ba/260414-g0q-SUMMARY.md` summarizing the two tasks, file changes, and verification results.
</output>
