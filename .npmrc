# Fixar versões exatas para evitar atualizações automáticas
save-exact=true

# Skip browser downloads - browsers run in separate containers
# Nota: Estas são variáveis de ambiente, não configurações do npm
# Configure-as no seu ambiente ou no .env.local
# PUPPETEER_SKIP_DOWNLOAD=true
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
