/**
 * Exemplo de teste unitário para o template de feature
 * 
 * Este arquivo serve como exemplo de estrutura de teste.
 * Remova ou substitua por testes reais da sua feature.
 */

import { describe, it, expect } from '@jest/globals';

describe('Feature Template Example', () => {
  it('should be a placeholder test', () => {
    expect(true).toBe(true);
  });
});
